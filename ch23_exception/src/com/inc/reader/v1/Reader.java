package com.inc.reader.v1;

import java.io.BufferedReader;
import java.io.FileReader;

public class Reader {
		//운영체제의 파일을 읽어 내용을 표시하는 클래스
		//예외처리에 중점
	public void run(String filename) {
		/*BufferedReader bReader = new BufferedReader(new FileReader(filename));
		//FileNotFoundException => 체크예외기 때문에 컴파일 에러에 걸림
		String input = bReader.readLine(); //IOException도 체크예외기 때문에 컴파일에 걸림
		System.out.println(input);*/
	}

}
